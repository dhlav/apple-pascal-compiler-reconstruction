Apple Pascal 1.1-APPLE3 HAZELGOTO.CODE -- lifted to pseudo-Pascal

Storage is named as it is addressed, not invented: G<n> is global
word n, L<n> local word n, I<lex>,<n> intermediate. See
docs/FINDINGS.md for what the numbered storage actually is.
A '{ stack tracking stopped }' marker means an instruction of
unknown stack effect was reached; the rest of that block is
listed verbatim.

======================================================================
SEGMENT 0 GOXY
======================================================================

procedure GOXY.1(args 0 words);  { locals 0 words, lex 255 }
  XIT;  { exit the interpreter }
  { left on stack: 0 }

procedure GOXY.2(args 2 words);  { locals 2 words, lex 0 }
  if (G2 > 79) then begin
    G2 := 79;
  end else begin
    if (G2 < 0) then begin
      G2 := 0;
    end;
  end;
  if (G1 > 23) then begin
    G1 := 23;
  end else begin
    if (G1 < 0) then begin
      G1 := 0;
    end;
  end;
  @G3^[0] := 126;
  @G3^[1] := 17;
  if (G2 < 30) then begin
    @G3^[2] := (G2+96);
  end else begin
    @G3^[2] := G2;
  end;
  @G3^[3] := (G1+96);
  UNITWRITE(2, @G3, 0, 4, 0, 0);
