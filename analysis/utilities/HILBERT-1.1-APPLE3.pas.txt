Apple Pascal 1.1-APPLE3 HILBERT.CODE -- lifted to pseudo-Pascal

Storage is named as it is addressed, not invented: G<n> is global
word n, L<n> local word n, I<lex>,<n> intermediate. See
docs/FINDINGS.md for what the numbered storage actually is.
A '{ stack tracking stopped }' marker means an instruction of
unknown stack effect was reached; the rest of that block is
listed verbatim.

======================================================================
SEGMENT 1 HILBERT
======================================================================

procedure HILBERT.1(args 2 words);  { locals 6 words, lex 0 }
  goto L018E;
L0088:
  TURTLEGR.1();
  FWRITESTRING(I1,3, 'WELCOME TO HILBERT', 0);
  IOCHECK();
  FWRITELN(I1,3);
  IOCHECK();
  FWRITESTRING(I1,3, 'ENTER ORDER 0 TO QUIT.', 0);
  IOCHECK();
  FWRITELN(I1,3);
  IOCHECK();
  repeat
    FWRITESTRING(I1,3, 'ORDER: ', 0);
    IOCHECK();
    FREADINT(I1,2, @G6);
    IOCHECK();
    FREADLN(I1,2);
    IOCHECK();
    if ((G6 < 1) or (G6 > 7)) then begin
      FWRITESTRING(I1,3, "THAT'S ALL FOLKS...", 0);
      IOCHECK();
      FWRITELN(I1,3);
      IOCHECK();
      EXIT(1, 1);
    end;
    TURTLEGR.2();
    G4 := 2;
    G3 := 2;
    G8 := G6;
    while (G3 <= G8) do begin
      G4 := (G4*2);
      G3 := (G3+1);
    end;
    G4 := (G4-1);
    G5 := (190 div G4);
    G4 := ((G4*G5) div 2);
    TURTLEGR.6((140-G4), (96-G4));
    TURTLEGR.7(1);
    HILBERT.2(G6);
    FREADCHAR(I1,4, @G7);
    IOCHECK();
    TURTLEGR.8();
  until 0;
  UNLOADSEGMENT(20);
  UNLOADSEGMENT(21);
  goto L0196;
L018E:
  LOADSEGMENT(21);
  LOADSEGMENT(20);
  goto L0088;
L0196:

procedure HILBERT.2(args 1 words);  { locals 2 words, lex 1 }
  if (L1 = 0) then begin
    TURTLEGR.3(180);
  end else begin
    if (L1 > 0) then begin
      L3 := 90;
      L2 := (L1-1);
    end else begin
      L3 := -(90);
      L2 := (L1+1);
    end;
    HILBERT.3();
    HILBERT.4();
    HILBERT.3();
  end;

procedure HILBERT.3(args 0 words);  { locals 0 words, lex 2 }
  TURTLEGR.3(I1,3);
  HILBERT.2(-(I1,2));
  TURTLEGR.3(I1,3);

procedure HILBERT.4(args 0 words);  { locals 0 words, lex 2 }
  TURTLEGR.5(G5);
  HILBERT.2(I1,2);
  TURTLEGR.3(-(I1,3));
  TURTLEGR.5(G5);
  TURTLEGR.3(-(I1,3));
  HILBERT.2(I1,2);
  TURTLEGR.5(G5);
