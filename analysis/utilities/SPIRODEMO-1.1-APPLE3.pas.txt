Apple Pascal 1.1-APPLE3 SPIRODEMO.CODE -- lifted to pseudo-Pascal

Storage is named as it is addressed, not invented: G<n> is global
word n, L<n> local word n, I<lex>,<n> intermediate. See
docs/FINDINGS.md for what the numbered storage actually is.
A '{ stack tracking stopped }' marker means an instruction of
unknown stack effect was reached; the rest of that block is
listed verbatim.

======================================================================
SEGMENT 1 SPIRODEM
======================================================================

procedure SPIRODEM.1(args 2 words);  { locals 5 words, lex 0 }
  goto L018E;
L0088:
  APPLESTU.1();
  TURTLEGR.1();
  G7 := 1;
  FWRITESTRING(I1,3, 'WELCOME TO WHILEPLOT', 0);
  IOCHECK();
  FWRITELN(I1,3);
  IOCHECK();
  FWRITESTRING(I1,3, 'ENTER ANGLE 0 TO QUIT.', 0);
  IOCHECK();
  FWRITELN(I1,3);
  IOCHECK();
  FWRITELN(I1,3);
  IOCHECK();
  repeat
    FWRITELN(I1,3);
    IOCHECK();
    FWRITESTRING(I1,3, 'ANGLE: ', 0);
    IOCHECK();
    FREADINT(I1,2, @G4);
    IOCHECK();
    FREADLN(I1,2);
    IOCHECK();
    if (G4 = 0) then begin
      SPIRODEM.3();
    end;
L011F:
    FWRITESTRING(I1,3, 'CHANGE: ', 0);
    IOCHECK();
    FREADINT(I1,2, @G3);
    IOCHECK();
    FREADLN(I1,2);
    IOCHECK();
    if not ((G3 > 0)) then goto L011F;
    repeat
      G5 := 10;
      TURTLEGR.2();
      TURTLEGR.7(1);
      while (G5 < 250) do begin
        SPIRODEM.2();
      end;
      TURTLEGR.10(3);
      TURTLEGR.10(3);
      G4 := (G4+15);
    until APPLESTU.5 KEYPRESS();
    FREADCHAR(I1,4, @G6);
    IOCHECK();
    TURTLEGR.8();
  until 0;
  UNLOADSEGMENT(22);
  UNLOADSEGMENT(20);
  UNLOADSEGMENT(21);
  goto L0199;
L018E:
  LOADSEGMENT(21);
  LOADSEGMENT(20);
  LOADSEGMENT(22);
  goto L0088;
L0199:

procedure SPIRODEM.2(args 0 words);  { locals 0 words, lex 1 }
  TURTLEGR.5(G5);
  TURTLEGR.3(G4);
  G5 := (G5+G3);
  if ((G5 mod 40) = 0) then begin
    if (G7 = 12) then begin
      G7 := 1;
    end else begin
      G7 := (G7+1);
    end;
L0023:
    { paths disagree on stack depth here }
    if not ((564 in 1)) then goto L0032;
    G7 := (G7+1);
    goto L0023;
  end;
L0032:
  { paths disagree on stack depth here }
  TURTLEGR.7(G7);

procedure SPIRODEM.3(args 0 words);  { locals 0 words, lex 1 }
  TURTLEGR.8();
  FWRITELN(I2,3);
  IOCHECK();
  FWRITESTRING(I2,3, "THAT'S ALL FOLKS...", 0);
  IOCHECK();
  FWRITELN(I2,3);
  IOCHECK();
  EXIT(1, 1);
