# The Apple Pascal 1.3 disk set

What every file is for, and how much of it has been reconstructed. The
descriptions in the first section are the user's, kept verbatim. The status
column and the second section are this repo's.

The machine-readable companion is `analysis/diskset-inventory.txt`,
regenerated by `tools/diskmap.py` on every build: every file on all six
evidence disks with its per-segment breakdown -- dictionary slot, segment
number, SEGINFO version, mtype, size, and how many procedures are p-code and
how many are native 6502. That file is generated and this one is not; where
they disagree, the generated one is right.

**The target is 1.3 and the 128K system** (`128K.APPLE` + `128K.PASCAL`).
The 64K `SYSTEM.PASCAL` and both 1.1 disks stay in `evidence/` and stay in
use: 1.1 ships source for utilities 1.3 ships only as codefiles, which is
how `LINEFEED` was reconstructed (finding 99a), and comparing the releases
is what says which files Apple actually rebuilt (finding 99c).

## Core System Files

| file | what it is | status |
|---|---|---|
| SYSTEM.APPLE | Boot files that configure the Apple hardware. | not started; raw 6502, not a codefile |
| SYSTEM.PASCAL | The main operating system and command menu. | **in progress -- 110 of 111 procedures instruction- and frame-identical to Apple's shipped `128K.PASCAL`, verified by `probe_os_exact.py` against a kept acceptance run of Apple's own compiler (finding 232). Segment by segment: `PASCALSY` 57/58, `GETCMD` 27/27, `INITIALI` 11/11, `PRINTERR` 1/1, `USERPROG` 1/1, `FILEPROC` 8/8, `FIOPRIMS` 5/5. `FIOPRIMS` is now written as the `UNIT` its `SEGKIND` says it is, declared inline and `USES`d with no library round trip (finding 232). The one procedure left in that run is `FGET`, and finding 232b shows it **cannot** be reached in a single compilation of a single file: its `USES` would empty `PROCTABLE[2..5]` after `EXECERROR`'s body had filled slot 2, and its own completion cannot move earlier because its nested `EXECGETCH` must claim procedure 56. `FGET` is written and verified all the same, in a second kept run that trades those four for it (finding 233) -- so **all 111 procedures have been produced byte-for-byte by Apple's own compiler**, and `probe_os_exact.py` checks the union of the two runs is 111. That claim briefly stood at 108: making jump destinations part of the comparison (finding 252) showed `PASCALSY.2`, `.33` and `.55` each send one jump to the wrong instruction while emitting Apple's instructions in Apple's order, and finding 253 fixed all three first attempt -- `EXECERROR` had the CONTROL-RESET hang on the inner `IF`'s `ELSE` rather than the outer one's, `SCANTITLE` assigned its result inside an `ELSE` so a plain title returned `FALSE`, and `BLKXFER` left one statement outside a `THEN`. The count is 111 again and now rests on a comparison that can see control flow. The `FGET` run is derived from the live source by `tools/fgetvariant.py` rather than kept as a snapshot, so a fix to a procedure both runs contain lands in both. The shipped dictionary says the same thing from the other side -- `SEGKIND` 6 with `TEXTADDR` 0, and a slot 15 no compile writes -- so `128K.PASCAL` was not produced by one compile. The narrative below is the history and is not kept current.** Target is 128K.PASCAL specifically (`src/pascal/os/1.3/PASCALSYSTEM.text`, findings 136-148); 7 segments, 105-111 procedures depending on build. The skeleton compiles clean under Apple's own `SYSTEM.COMPILER` first attempt: `CONST`/`TYPE`/`VAR` ported from UCSD II.0's `GLOBALS.TEXT`, its own 41 forward-declared segment-0 procedures (27 "fixed", 14 "non-fixed"), and all 7 segments in their real disk slots. `USERPROG` (segment 1) and `PRINTERR` (segment 3, `params=4/data=46`, Apple reworded its messages and added 128K ProFile-specific error codes) are both frame-exact, straight from UCSD's own source once written as direct `FWRITELN`/`FWRITESTRING` calls (`WRITE`/`WRITELN`'s own sugar only accepts a plain variable, not a pointer dereference, on Apple's real compiler). `SCONCAT`/`SINSERT`/`SCOPY`/`SDELETE`/`SPOS` (segment 0, what `CONCAT`/`INSERT`/`COPY`/`DELETE`/`POS` sugar lowers to) are all five exact too -- Apple's real parameter *order* for these diverges from UCSD's own literal text, recovered by reading each body's own algorithm directly, not assumed (finding 138). `FGOTOXY` is close (`params=4/data=0` against real `4/2`, one documented word -- Apple's own body caches a pointer this one re-derives instead, finding 140). 42 of segment 0's 43 procedures now match Apple's real `params` exactly. `PASCALSY`'s own 451-word global `VAR` section -- what nearly every other remaining procedure depends on -- is started (263 of 451 words currently declared): the real `SYSCOMREC` type, and `SYSCOM` (offset 1) plus `GFILES[1]` (offset 3, the real target of every segment-0 body's own console-output call) confirmed by reading `FGOTOXY`'s own real body directly (finding 139). `MAXUNIT` corrected from UCSD II.0's own `12` (1.1's value) to `20` (1.2/1.3's, per Neil Parker's documented `DISKNUM` table, finding 141), widening `UNITABLE` by 48 words. `MAX_SEG` also corrected, 31 -> 63 (same Parker annotation, finding 142) -- widens `SYSCOMREC.SEGTABLE`, a heap structure reached through the `SYSCOM` pointer, so it does not move `PASCALSY.1`'s own frame at all (confirmed inert, both compilers agree). **The 451-word `VAR` section is now closed exactly** (finding 143): Parker's document transcribes every field Apple added past UCSD II.0's own globals (Dave Tribby's reverse-engineered names, `CONFIG_CHAR` through `WHAT_L`), self-consistent against this file's own word-size formulas with zero exceptions across ~26 fields; procedure 1 now compiles to `params=0/data=902`, an **exact match** to Apple's real `128K.PASCAL`, on both the host compiler and Apple's own. Field boundaries are verified by the frame-size match; individual field identities (beyond `SYSCOM`/`GFILES[1]`) remain STRONG INFERENCE. `PASCALSY.1`'s own main-loop body is started (finding 144): `EMPTYHEAP := NIL`, `UNITCLEAR(1)`, `INITIALIZE` written and verified exact. `FINIT`/`FCLOSE` now resolved (finding 145): the mystery file variable is `EXEC_FILE: FILE`, an untyped 40-word file collapsing seven of finding 143's own Tribby-guessed fields -- Apple's own compiler auto-generates both calls from the `VAR` declaration alone, confirmed byte-for-byte. Procedures 44-47 (findings 146/147) -- the EXEC-file buffered I/O layer -- now written and verified exact too. Procedure 50 (`WAITSYSVOL`, finding 148) is now written as well -- `params=0` exact, `data` documented one word short (likely tied to an unresolved `GFILES[1]^`-addressing question); `48`/`49` are declared as number-preserving stubs only, since reaching `PASCALSY.58` (which `48`'s own real loop calls) needs `51`-`57` declared first, six of which are lexically nested one level inside a not-yet-identified enclosing procedure -- real, separate work. That nested-scope group resolved into four independent pairs by caller-graph analysis (finding 149); the `G<n>`/`FIB` labeling trap in the lift for `lex 0` segment-0 procedures' own frames was found and corrected (finding 150), cross-corroborated against `SYSTEM.LIBRARY`'s own `PASCALIO.text` (finding 151), which also confirmed the Tribby/`MAXUNIT`/`MAX_SEG` changes stay inert there by design (same finding). `PASCALSY.51`/`52` (the `"S#/P#/I#"` and `"Execution error #"` diagnostic printers nested inside `EXECERROR`) and `EXECERROR` itself (`PASCALSY.2`, whole body) are now written for real and VERIFIED BINARY FACT, read from `128K.PASCAL`'s own raw p-code directly rather than the lift's abstracted rendering (findings 153/154) -- disassembling instruction-for-instruction identical to the real binary apart from two flagged, expected fast-tier compiler-codegen differences. Still stubs: `EXECCLOSE`, `SPACEWAIT`, `FETCHDIR`'s own real bodies, `PRINTERROR`'s real per-code message table, and `PASCALSY.1`'s own main `REPEAT ... UNTIL EMPTYHEAP = NIL` loop (which calls `PASCALSY.48`, also still a stub), along with every other forward-declared procedure's real content and four of the six other segments' content beyond procedure 1. `FINIT`/`FEOF`/`FEOLN` written for real (finding 155); `FILEPROC`'s real six-word shared dispatch signature discovered and its arm-1/2/3/4/7 routing written, unblocking `FRESET`/`FOPEN`/`FCLOSE` as real forwarders (findings 156-157, a new parameter-clause-reversal rule found along the way). `FBLOCKIO`'s nested chunked-transfer loop (`PASCALSY.55`, written as `BLKXFER`) matches the real binary instruction-for-instruction; `FBLOCKIO`'s own outer body and its file-extension helper `STUB49` are both now written whole too (findings 158-165), resolving `FIB`/`DIRENTRY`'s real packed-field offsets by probe (twice correcting a hand-counted first guess) and a `CBP`/`CGP` call's own result-reservation convention along the way -- both compile clean but, unlike `BLKXFER`, could not be verified instruction-for-instruction due to a fast-tier quirk where the host compiler omits them from their own procedure table for reasons not tracked down; labelled STRONG INFERENCE pending the acceptance tier. Still stubs: `FILEPROC`'s own arm bodies (2/3/4/7), `FGET`/`FPUT` and the rest of the `FIB` read/write layer, the whole `FIOPRIMS` segment, `SPACEWAIT`/`FETCHDIR`'s real bodies, `PRINTERROR`'s real per-code message table, and `PASCALSY.1`'s own main loop |
| SYSTEM.MISCINFO | Screen and keyboard setup data for your terminal. | not started; written by SETUP |

## Main Utilities

| file | what it is | status |
|---|---|---|
| SYSTEM.EDITOR | The text editor used to write code. | not started; 129 procedures, the largest single target |
| SYSTEM.FILER | The file manager used to copy, list, and delete files. | not started; 56 procedures, one segment |
| SYSTEM.COMPILER | Translates your Pascal code into machine-readable code. | 147 of 147 procedures verified (finding 90); linked, all 15 real segments byte-identical including `PASCALCO` (finding 107); short only an extra, empty host segment Apple's shipped file does not have (finding 105a) |
| SYSTEM.LINKER | Combines different compiled code pieces into one program. | not started; 51 procedures |
| SYSTEM.ASSMBLER | Low-level assembly language translator. | skeleton compiled by Apple's own compiler, 0 errors (finding 237): six segments with Apple's names, numbers and procedure counts, all 90 shared signatures agreeing, 83 of 95 bodies byte-exact (findings 238-263); **five of the six segments are complete** -- `ASSEMBLE` (33), `PROCEND` (9), `INITIALI` (6), `SYMTBLDU` (3), `PRINTERR` (1) -- and only `TLA` remains, 31 of 38. `INITIALI.2`'s 33-entry directive table names every token code the rest of the file compares against and confirmed every handler already written. `TLA` is a SEGMENT PROCEDURE under `(*$U-*)` on segment 1, the five nested ones on 7-11, the global area balances at 2217 words. The `PASCALIO` segment cannot be rebuilt from anything on the 1.3 disks (finding 235b). Also the acceptance tier for `src/native/` (finding 44e) |

## Libraries and Code Tools

| file | what it is | status |
|---|---|---|
| SYSTEM.LIBRARY | Default code library for running basic graphics and tools. | **reconstructed**, 55 p-code and 14 native procedures (tag `system-library-1.3-complete`) |
| LIBRARY.CODE | Utility to add or remove tools from library files. | **in progress, no procedure left unwritten**; 16 procedures. Compiles clean under Apple's own compiler with the right structure (finding 113), and under this project's own reconstructed compiler byte-identically to Apple's (finding 114). `SWAPBYTES`/`CHECKIO`/`GETINPUT`/`COPYSLOT`/`GETCOMMAND`/`COPYLINK`/`READLINK`/`COPYINTERFACE`/`IFACEWERR` frames exact (findings 115, 116); `NEEDSSWAP`/`SWAPALL`/`MSGLINE`/`MSGLINEINT`/`SHOWDICT` close (a few words short, documented); `MAINLOOP` has an unexplained 74-word gap (finding 114), the one open thread left in this file |
| LIBMAP.CODE | Shows you what is inside a library file. | **reconstructed** (`src/pascal/programs/1.3/LIBMAP.text`, findings 109/111/120/121); 12 procedures, every one with a real body, nothing stubbed. Native half free (finding 109 -- `LIBMAP.2` is Apple's own `IDSEARCH`, reused whole from `SYSTEM.COMPILER`). Exact matches: outer block, `SWAPBYTES`, `VALIDNAME`, `SHOWSEGS`, `SHOWONE`, `GETWORD`, `SHOWINFO` (the largest procedure in the file at 1042 bytes of locals, matched first attempt via a 7-word `IDSEARCH` cursor record, finding 121). One word short each, documented not forced: `NEEDSSWAP` (two words), `SWAPALL`, `SHOWREF`, `MAPLIBRARY` |
| BINDER | Joins assembly language parts into Pascal programs. | **reconstructed** (`src/pascal/programs/1.3/BINDER.text`, findings 117-118), no shipped source existed anywhere on the evidence disks. 6 procedures, compiled clean under Apple's own `SYSTEM.COMPILER` after a handful of real fixes; five of six procedures' frames exact, the outer program body short by one word out of 1119 -- chased with `SYSTEM.COMPILER`'s own disassembly, no candidate found, documented not forced (finding 118). A 1.1-vintage binary Apple's 1.3 disk never rebuilt (finding 99c); confirmed it runs under 1.3 off `SYSHD:`, so it's in scope |
| SET40COLS | Sets the 40/80-column boot flag in a Pascal volume's directory. | **reconstructed** (`src/pascal/programs/1.3/SET40COLS.text`, finding 112), no shipped source existed anywhere on the evidence disks. Compiled clean under Apple's own `SYSTEM.COMPILER` first attempt, all four procedures' frames exact, full instruction diff 469/469; codefile 2544/2560 bytes identical -- the last 16 are the same SEGINFO version-stamp mystery finding 99c found in `BINDER` (both are 1.1-vintage binaries Apple's 1.3 disk never actually recompiled), not reachable until that's explained |

## Configuration and Setup

| file | what it is | status |
|---|---|---|
| SETUP.CODE | Changes hardware settings like your screen size. | **in progress** (`src/pascal/programs/1.3/SETUP.text`, findings 122-123, 126-130, 132-135); 54 procedures in 12 segments, nine of them 16-byte stubs. Byte-identical between 1.1 and 1.3. All 54 compile clean under Apple's own `SYSTEM.COMPILER`. Frame-exact: the outer body, `SETUP2`, `SETUP3`, `SETUP5`, `SETUP6`, `SETUP7`, `SETUP9`, `SETUP10`, `SETUP11`, `SETUP12`, `SETUP13`, `SETUP14`, `SETUP15`, `SETUP17`, `SETUP18`, `SETUP20`, `SETUP21`, `SETUP22`, `SETUP23`, `SETUP24`, `SETUP25`, `SETUP26`, and all eight `NUMBERn` stubs. `SETUP4` closed from an 82-word miss to one word (`VAR SRC`, not value, was the real fix, finding 123); the remaining word is documented not forced. `SETUP16` is one word short too, documented not forced. Finding 126 discovered `SETUP13`-`26` are nested *inside* `SETUP12`, not flat siblings of it; finding 127 rebuilt it that way and every nested procedure landed exactly on Apple's own numbers and frame sizes, no `FORWARD` needed; finding 128 filled in the scalar-value display/list/lookup trio (`22`/`23`/`24`, reusing SETUP3's own sentinel-search trick) plus `SETUP20`'s `2^N`, all four exact first attempt; finding 129 filled in `SETUP17`/`18` (bit-field pack/unpack, reusing SET40COLS.text's own variant-record bit-view idiom for `MISCINFO`), both exact; finding 130 filled in `SETUP9`/`11` (the octal/decimal/hex read-out line and the entry-format help screen), both exact; finding 132 filled in `SETUP10` (the numeric-entry reader, `params=12/data=128` exact) -- a first attempt's `STRING[70]` entry buffer, sized to a guessed exact fit, was 5 words short; Apple's real buffer is a plain default `STRING` (80 chars), bigger than the tightest guess that would fit. Finding 133 filled in `SETUP8` (the QUIT handler, the largest procedure in the file at 398 words) -- D(isk)/H(elp)/E(xit) all exact (`EXIT(SETUP)`, not `EXIT(PROGRAM)`/`EXIT(PASCALSYSTEM)`, both of which fail), M(emory)-update left an intentional stub (a cross-segment write into `PASCALSYSTEM`'s own implicit scope, unreachable until `SYSTEM.PASCAL` itself is reconstructed) -- 2 words short (`792` vs `796`), documented not forced. Finding 134 caught a real rule (a `VAR`-parameter group descends like a `VAR`-block local, but a plain-value-parameter group ascends -- the opposite) while attempting `SETUP19`, whose own 41-word frame gap stayed open and was reverted rather than shipped. Finding 135 filled in `SETUP21` (the scalar-name entry driver, `params=0/data=84` exact) -- needed its own local `VALUE`/`ENTRY: STRING` (41 words), unlike `SETUP19`, which delegates all text entry to `SETUP10` and never owns a buffer of its own. `SETUP19` and `TEACHSET` (tutorial text) are the only two stubs left in the whole file |
| FORMATTER.CODE | Utility used to format blank floppy disks. | **reconstructed**, Pascal and 6502 both, compiled/assembled/linked by Apple's own tools to an identical codefile (finding 104) |
| FORMATTER.DATA | Support data used by the disk formatter. | not started; data file |

## Assembly Reference Files

| file | what it is | status |
|---|---|---|
| 6502.OPCODES | List of commands for the Apple II processor chip. | not started; 720 bytes, read by SYSTEM.ASSMBLER |
| 6502.ERRORS | List of error messages for the assembly tool. | not started; 3570 bytes |

## Also on the disks

The list above is the interesting half. The disks carry more:

* **`128K.APPLE` and `128K.PASCAL`** (APPLE3) -- the 128K system, and the
  build target; see `SYSTEM.PASCAL`'s own row above for reconstruction
  status (`src/pascal/os/1.3/PASCALSYSTEM.text` targets `128K.PASCAL`
  directly, not the 64K build). Four of `128K.PASCAL`'s seven segments
  were already known byte-identical to the 64K build's own -- `USERPROG`,
  `FIOPRIMS`, `PRINTERR` and `FILEPROC` -- a fact recorded here for the
  record, not a reason to read or cite that build going forward.
* **`LINEFEED.CODE`** (APPLE3) -- suppresses line feeds. One procedure, 38
  bytes, and **reconstructed** (finding 99a).
* **`SYSTEM.CHARSET`** (APPLE1) -- the hi-res character set, 1024 bytes.
* **`SYSTEM.SYNTAX`** (APPLE1) -- the compiler's error messages. Already a
  text file on the disk, so it is reproduced by writing the volume.
* **`II40.MISCINFO`, `II80.MISCINFO`, `HAZEL.MISCINFO`** (APPLE3) -- the
  terminal profiles SETUP writes.
* **Eleven `.TEXT` sample programs** (APPLE3) -- `BALANCED`, `CROSSREF`,
  `DISKIO`, `GRAFCHARS`, `GRAFDEMO`, `HILBERT`, `SPIRODEMO`, `TREE`,
  `HAZELGOTO`. Already source on the disk. The 1.1 APPLE3 disk ships the
  matching `.CODE` for each, which makes them a calibration corpus: Apple's
  source next to Apple's own output.

## What is left, by size

Counting procedures rather than bytes, and 1.3 only: 216 done of roughly
861. Every remaining target can be read before it is written: the sweep
covers the codefiles a 1.3 disk carries, plus the 1.1 copies of those same
files, and **it lifts 1147 of 1147 with the stack fully tracked**
(finding 100). The listings are in `analysis/utilities/`, p-code and
pseudo-Pascal side by side.

Files that ship only on a 1.1 disk -- `CALC.CODE` and the demo programs'
codefiles -- are out of *scope*, but they are still swept and still listed
in `analysis/utilities/`. Nothing is discarded: the demos' 1.1 `.CODE`
beside their `.TEXT` is the only source-and-output pair Apple left behind.
