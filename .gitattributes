# The third-party build recipe runs under WSL/Linux; CRLF breaks both a
# shell script's shebang handling and `patch`'s context matching.
thirdparty/**/*.sh   text eol=lf
thirdparty/**/*.patch text eol=lf
*.tar.gz             binary
