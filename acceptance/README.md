# Acceptance runs

What Apple's own tools produced, kept as it came off the emulator.

Everything else in this repository is either evidence (Apple's shipped
media, never modified) or generated (`build/`, `analysis/`,
`reference_source/`, rebuilt by `tools/build_all.py`). This is neither. An
acceptance run happens inside AppleWin, driven by hand through
`tools/emucompile.ps1`, `tools/emuassemble.ps1` or `tools/emulink.ps1`, and its output would be
gone the next time `WORK2.dsk` is rewritten. So the output is kept here, and
a probe under `tools/probes/` compares it against the shipped binary on
every build -- which turns a one-off emulator session into a check that goes
on being run.

One directory per run: the codefile Apple's tool wrote, and the screen at
the moment it finished.

| run | what was run | result |
|---|---|---|
| `2026-08-24-formatter-native` | `SYSTEM.ASSMBLER` 1.3 on `src/native/FORMATTR.TEXT` (as `FMTNATIV.TEXT`) | 225 lines, **0 errors**; 354 bytes **identical** to `FORMATTER.CODE`'s procedure 2, relocation table included (finding 103e) |
| `2026-08-24-formatter-linked` | `SYSTEM.COMPILER` 1.3 on `FORMATTER.text`, then `SYSTEM.LINKER` joining it to the above | the whole codefile **identical** to `FORMATTER.CODE` -- dictionary and all 2672 bytes of segment `FORMATTE` -- to the end of the segment (finding 104) |
| `2026-08-25-compiler-search-asm` | `SYSTEM.ASSMBLER` 1.3 on `src/native/SEARCH.TEXT` | 519 lines, **0 errors**; the assembled `IDSEARCH`/`TREESEARCH` are what the linked run below copies in |
| `2026-08-25-compiler-linked` | `SYSTEM.COMPILER` 1.3 on the fifteen-segment `BODY13.TEXT` splice (before the body-order fix), then `SYSTEM.LINKER` joining it to `2026-08-25-compiler-search-asm` | superseded by the `-v2` run below; kept for the record. 14 of 15 segments byte-identical, both native routines exact; `PASCALCO` differed by an extra empty host segment and a different physical body order (finding 105) |
| `2026-08-25-compiler-linked-v2` | same, after `PASCALCO.text`'s bodies were reordered and `COMPILE` given a forward declaration | **all 15 real segments byte-identical**, `PASCALCO` included (finding 107). Not wired into `probe_acceptance.py`: the codefile as a whole still carries the extra empty `PASCALSY` segment (finding 105a), so a whole-file compare cannot pass yet even though every real segment does |
| `2026-08-27-libmap-showone-getword` | `SYSTEM.COMPILER` 1.3 on `LIBMAP.text` (as `LIBMAPT.TEXT`, compile-only, not linked) | **0 errors**; procedure 8 (`SHOWONE`) `params=2/data=556` and procedure 10 (`GETWORD`) `params=2/data=2`, both **exact matches** to Apple's shipped `LIBMAP.CODE` (finding 111 update). Not wired into `probe_acceptance.py`: `LIBMAP.text` is still mid-reconstruction (`SHOWINFO`/`SHOWREF`/`MAPLIBRARY`/outer block are stubs), so no whole-file or whole-segment compare is possible yet -- this run is a frame-size check on two procedures, not a byte compare |
| `2026-08-27-set40cols-compile` | `SYSTEM.COMPILER` 1.3 on new source `SET40COLS.text` (as `SET40T.TEXT`, compile-only, not linked -- no native half, nothing to link) | **0 errors, first attempt**; all four procedures' `params`/`data` exact matches to Apple's shipped `SET40COLS.CODE`; **2544 of 2560 bytes byte-identical**, all 16 real differences the same SEGINFO version-stamp byte (`$42` vs `$C2`, i.e. version 2 vs version 6) finding 99c already found unexplained in `BINDER.CODE` -- confirms the same mystery, not a new gap (finding 112) |
| `2026-08-27-library-compile` | `SYSTEM.COMPILER` 1.3 on new source `LIBRARY.text` (as `LIBRARYT.TEXT`, compile-only, not linked -- no native half) | **0 errors** after several real fixes (reserved word `INTERFACE`, `NEW`'s variant-tag-not-flex-size semantics, `FILLCHAR`'s 3-argument syntax, and getting `COPYSLOT`/`GETCOMMAND`'s nesting to match the binary's own lex levels). All 16 procedures present with the right structure; `SWAPBYTES`/`CHECKIO`/`GETINPUT` frames exact, several more close within a few words (documented, not forced), the copy/link/interface logic (procedures 11-15) deliberately left `(*STUB*)` -- this is a checkpoint, not a finished reconstruction (finding 113) |
| `2026-08-28-selfhost-library` | This project's own **reconstructed** `SYSTEM.COMPILER` (`acceptance/2026-08-25-compiler-linked-v2/COMPLINK.CODE`, swapped onto `SYSHD:` in place of Apple's shipped one) compiling the same `LIBRARY.text`/`LIBRARYT.TEXT` as the row above | **Byte-identical output, all 3072 bytes, 0 differences** against the same source compiled by Apple's own shipped `SYSTEM.COMPILER` -- the first real, substantial (not a calibration program) test of the reconstructed compiler actually compiling something end to end. Confirms finding 113's `MAINLOOP` frame-size anomaly is not a bug in this project's `SYSTEM.COMPILER` reconstruction -- both compilers agree exactly -- so whatever explains it is a difference between this project's `LIBRARY.text` and Apple's true original source, not a compiler quirk to keep chasing (finding 114) |
| `2026-08-28-library-copyslot` | `SYSTEM.COMPILER` 1.3 on `LIBRARY.text` with `COPYSLOT` (procedure 11) written for real (heap-chained `MARK`/`NEW`/`RELEASE` buffer, `WITH INDICT.ADDRLEN[FROMSLOT] DO` in place of the disassembly's illegal `@`, `BLOCKREAD`/`BLOCKWRITE` through it, the two-dictionary field copies, and the `SEGKIND NOT IN [0,6,7]` gate on `COPYLINK`) | **0 errors, first attempt**; `COPYSLOT` now matches Apple's binary exactly on **both** `params=4` and `data=10` -- the `params` mismatch (this project had 6, from a `VAR ABORT: BOOLEAN` parameter) resolved by discovering `ABORT` isn't a parameter at all: the disassembly's own `I1,3 := 0` writes directly into `MAINLOOP`'s frame, meaning `COPYSLOT` takes only `(TOSLOT, FROMSLOT: INTEGER)` and reads/writes `MAINLOOP`'s `ABORT` by lexical scoping, same sharing pattern LIBMAP's `GETWORD`/`SHOWONE` already established (finding 115) |
| `2026-08-28-library-copylink` | `SYSTEM.COMPILER` 1.3 on `LIBRARY.text` with `COPYLINK`/`READLINK`/`COPYINTERFACE` (procedures 12-14) written for real | **0 errors, first attempt**, 839 lines; all four remaining stubbed procedures (`COPYLINK`, `READLINK`, `COPYINTERFACE`, `IFACEWERR`) now match Apple's binary exactly on both `params` and `data` -- `COPYLINK`'s `data=534` (a whole 256-word link-info block plus its own current-entry register) and `COPYINTERFACE`'s `data=6` landed exact on the first attempt once `COPYINTERFACE` was written to reuse `COPYSLOT`'s own `MARKPTR` local (finding 116) rather than declare its own |
| `2026-08-28-binder-compile` | `SYSTEM.COMPILER` 1.3 on new source `BINDER.text` (as `BINDERT.TEXT`, compile-only, not linked -- no native half) | **0 errors** after three real fixes (a value STRING parameter's own 41-word copy-on-entry, `NEW` without a variant tag on a plain array, an 8-character identifier collision) and two frame-size corrections found via the acceptance tier (a spurious explicit string-copy local, a spurious second `STRING[80]` local where the real source reuses the first). Five of the six procedures now match Apple's shipped binary exactly on both `params` and `data`; the sixth (the outer program body) is short by exactly one word out of 1119, documented not forced (finding 117) |
| `2026-08-28-binder-segnum-with` | `SYSTEM.COMPILER` 1.3 on `BINDER.text` with the `SEGNUM` loop's `DICT.ADDRLEN[SEGNUM]` reads wrapped in a real `WITH` (matching the disassembly's own `SRO 1121` address-reuse across `LOADSEG`/`WRITESEG`) | **0 errors**; structurally faithful fix, `4/2236` unchanged (no regression, doesn't close finding 117's one-word gap). Chased the gap itself using `SYSTEM.COMPILER`'s own reconstructed `DECLARAT.text` and every local offset `READGOTOXY` touches; ruled out the `FINIT` window address and a second hidden `CONCAT` temp as candidates. No candidate left open (finding 118) |
| `2026-08-28-libmap-showref-maplibrary` | `SYSTEM.COMPILER` 1.3 on `LIBMAP.text` (as `LIBMAPT.TEXT`, compile-only, not linked) with `SHOWREF`, `MAPLIBRARY`, and the outer block written for real, plus `NEEDSSWAP`'s parameter type fixed | **0 errors**; outer block `params=4/data=1510` **exact match**; `SHOWREF` `2/102` against Apple's `2/104` (one word short, documented); `MAPLIBRARY` `0/170` against Apple's `0/172` (one word short, documented) after catching that `CONCAT` (not `INSERT`) was costing a real 41-word hidden temp -- disassembling this project's own compiled output is what caught it, not just reading Apple's. `NEEDSSWAP` ported `LIBRARY.text`'s already-verified `ADDRLENARR`/`P[I].ADDR` fix onto a real call site (finding 120) |
| `2026-08-28-libmap-showinfo` | `SYSTEM.COMPILER` 1.3 on `LIBMAP.text` with `SHOWINFO` (procedure 9, the largest in the file) written for real -- the last stub | **0 errors, first attempt**; `params=6/data=1042`, **exact match** to Apple's binary. `IDSEARCH`'s own `VAR IDREC` argument turned out to be a 7-word cursor record (`IDX`, `SY`, and five unread words), not the 1-2 words its own declaration site in `PASCALCO.text` suggested -- that structure size is what closed the frame exactly (finding 121). `LIBMAP.text` now has a real body for every one of its twelve procedures, nothing left stubbed |
| `2026-08-28-setup-first-compile` | `SYSTEM.COMPILER` 1.3 on new source `SETUP.text` (as `SETUPT.TEXT`, compile-only, not linked) -- the skeleton, menu loop, field-tree/BST machinery, and the whole `INITS` startup segment | **0 errors**, all 54 procedures across 12 codefile segments (26 real/stubbed in `SETUP`, 8 trivial `NUMBERn`, 9 in `INITS`, `TEACHSET` a stub). Required discovering the real `(*$U-*) PROGRAM PASCALSYSTEM` / `SEGMENT PROCEDURE SETUP(P1,P2)` wrapping shape (`SYSTEM.COMPILER`'s own skeleton has the precedent) after `EXIT(PROGRAM)`/`EXIT(SETUP)` both failed under a naive single-`PROGRAM` shape, plus a `SEGMENT PROCEDURE`-after-a-real-body `CODEINSEG`/error 399 fix (declaration reordering, finding 119's own named mechanism) and the usual `NEW(p,n)` variant-tag trap (finding 113). Exact matches: the `SETUP` segment's own outer body (`4/352`), `SETUP2` (`4/82`), `SETUP3` (`6/164`), all eight `NUMBER2`..`NUMBER9` (`0/0`). `SETUP4` is a real, understood miss (finding 122) |
| `2026-08-28-setup-var-src` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP4`'s `SRC` parameter changed from value to `VAR` | **0 errors**; `SETUPT.CODE` extracted off `SYSHD:` and read back with `CodeFile` directly (not just the compiler's own summary screen) -- `SETUP4` `params=4/data=0` against Apple's `4/2` (closed from an 82-word miss to a one-word miss; the binary's own procedure 4 loads `SRC`'s address the same way it loads `DEST`'s, so it is `VAR`, not value -- finding 123). Also caught, by reading Apple's own dictionary directly: `SETUP5`/`SETUP6` were already exact at `4/0` each, correcting finding 122f's own wrong claim of a mismatch |
| `2026-08-28-setup7-exact` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP7` (the line-editor helper) written for real | **0 errors**, 651 lines; `SETUP7` `params=6/data=84`, **exact match** to Apple's binary, confirmed via `CodeFile` on the extracted `SETUPT.CODE`. A first attempt also wrote `SETUP12`/`13`/`14`/`15`/`25`/`26` as flat bodies with `FORWARD` used for the ones `SETUP12` calls -- compiled clean but numbered every procedure wrong from 7 onward, since `FORWARD` reserves a number immediately at declaration (finding 61's own "declaration order is the numbering," misparaphrased in this file's earlier comments) and since `SETUP13`-`26` are actually nested *inside* `SETUP12` in Apple's binary (lex 2-4 vs. `SETUP12`'s own lex 1), not flat siblings. Reverted 12-26 to stubs; only `SETUP7` shipped this run (finding 126) |
| `2026-08-28-setup-nested-change` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP12` rebuilt correctly nesting `SETUP13`-`26` inside it (13, 14 with 15 nested inside, 16 with 17-24 nested inside, 25, 26 -- 13/14/15/25/26 written for real, 16 and its own nested subtree left as empty stubs) | **0 errors**, 837 lines, first attempt at the corrected structure. `SETUP12`/`13`/`14`/`15`/`25`/`26` all **exact** against Apple's binary on both `params` and `data`, with no `FORWARD` declaration needed anywhere -- confirms finding 126's nesting hypothesis and finding 61's real rule (declaration order is the numbering) at once (finding 127) |
| `2026-08-28-setup-scalar-editor` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP20` (`2^N`) and the scalar-value display/list/lookup trio `SETUP22`/`23`/`24` written for real, nested inside `SETUP16`'s own subtree | **0 errors**, 915 lines; all four **exact** against Apple's binary on both `params` and `data` (`SETUP20` `6/6`, `SETUP22` `2/2`, `SETUP23` `0/2`, `SETUP24` `8/84`), first attempt. `SETUP24` reuses SETUP3's own sentinel-search idiom (writing the search key into `BOOLNAMES^.NAME` first so the loop can't fail to terminate) -- a second, independent confirmation of that trick (finding 128) |
| `2026-08-28-setup-bitfield` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP17`/`SETUP18` (bit-field pack/unpack) written for real | **0 errors**, 966 lines; both **exact** against Apple's binary (`2/6` each) after adding a local `FIELD := L1` copy of `SETUP16`'s own parameter (first attempt without it was 2 bytes short each, `2/4`) -- `MISCREC`'s element type redeclared as a variant record overlaying `INTEGER` with a `PACKED ARRAY[0..15] OF 0..1` bit view, the same idiom already compiled clean in `SET40COLS.text`'s own `FLAGBYTE` (finding 129) |
| `2026-08-28-setup-display-help` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP9` (octal/decimal/hex read-out) and `SETUP11` (entry-format help screen) written for real | **0 errors**, 1117 lines; both **exact** against Apple's binary (`SETUP9` `4/8`, `SETUP11` `6/2`). `SETUP9` needed two separate `OCTVIEW`/`HEXVIEW` variant locals plus an explicit plain-integer copy, not one combined 3-way variant (a first attempt was 4 bytes short); `SETUP11` needed an explicit `KEY: CHAR` local capturing `SETUP5`'s own result rather than a bare call in the loop condition (finding 130) |
| `2026-08-29-setup10-numeric-entry` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP10` (the numeric-entry reader) written for real | **0 errors**, 1244 lines; **exact match** to Apple's binary (`params=12/data=128`), confirmed via `CodeFile` on the extracted `SETUP.CODE` and a full 26-procedure diff against Apple's shipped file (only the pre-existing, already-documented gaps remain: `SETUP4`, `SETUP16`, and stubs `SETUP8`/`SETUP19`/`SETUP21` -- nothing regressed). A first attempt declared the entry buffer as `STRING[70]` (a guess at the exact remaining word budget) and came out 5 words short (`118` vs `128`); Apple's real buffer is a plain default `STRING` (80 chars, 41 words) -- the entry buffer is bigger than the tightest guess that would fit, not sized to exactly consume the frame (finding 132) |
| `2026-08-29-setup8-quit-handler` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP8` (the QUIT handler, the largest procedure in the file) written for real | **0 errors**, 1301 lines; `params=0/data=792` against Apple's `0/796` -- 2 words short, documented not forced (the M(emory)-update branch's own cross-segment write into `PASCALSYSTEM`'s own implicit scope, left an intentional `(*STUB*)` since `SYSTEM.PASCAL` isn't reconstructed yet). D(isk)/H(elp)/E(xit) all written for real; a throwaway probe compile (not part of this file) empirically measured a bare `FILE OF MISCREC` local at exactly 396 words, confirming the rest of the frame is accounted for. `EXIT(PASCALSYSTEM)` and the bare `EXIT(PROGRAM)` both failed (errors 104 and 125) -- `EXIT(SETUP)` is the form this file already established works, matching `SETUP2`'s own precedent. Full 26-procedure diff confirmed nothing else regressed (finding 133) |
| `2026-08-29-setup19-attempt-reverted` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP19` (the numeric entry driver) written and tested | **0 errors**, but `params=2/data=6` against Apple's real `2/88` -- a 41-word gap far too large to be a documented-not-forced miss, so **reverted to a stub** rather than shipped. Surfaced a real rule along the way (finding 134a): a `VAR`-parameter group descends like a `VAR`-block local, but a plain-value-parameter group *ascends* -- the opposite, confirmed by comparing `SETUP4`'s and `SETUP11`'s own already-exact signatures directly. The 41-word gap itself stayed open (finding 134b) -- not part of this file's shipped state |
| `2026-08-29-setup21-scalar-driver` | `SYSTEM.COMPILER` 1.3 on `SETUP.text` with `SETUP21` (the scalar-name entry driver) written for real | **0 errors**, 1345 lines; **exact match** to Apple's binary (`params=0/data=84`). Needed its own local `VALUE: INTEGER` and `ENTRY: STRING` (41 words, matching `1+41=42` exactly) -- named to avoid shadowing `SETUP16`'s own `L1` and `SETUP17`'s `RESULT` that `SETUP22`/`23`/`24` still reach by lexical scoping. Full 26-procedure diff confirmed nothing else regressed; only `SETUP19` and `TEACHSET` remain unwritten in the whole file (finding 135) |
| `2026-08-29-pascalsystem-skeleton` | `SYSTEM.COMPILER` 1.3 on new source `PASCALSYSTEM.text` (`src/pascal/os/1.3/`) -- the first `SYSTEM.PASCAL` session: `CONST`/`TYPE`/`VAR` ported from UCSD II.0's `GLOBALS.TEXT`, its own 41 forward-declared segment-0 procedures, and all 7 segments (`USERPROGRAM` real, five stubs) | **0 errors, first attempt at the restructured file**, 402 lines; every segment landed in its real disk slot with its real name, and **42 of segment 0's 43 procedures already match Apple's real `128K.PASCAL` on `params`** (the one mismatch is procedure 43, already known not to be `COMMAND`, finding 51c). `USERPROGRAM` itself matches Apple's real `args 2 words`. Chased down a real Apple-compiler quirk to get here: `WRITE`/`WRITELN`'s own sugar only accepts a plain variable identifier as its file argument, not a pointer dereference -- confirmed directly (`WRITELN(SYSTERM^)` fails, `WRITELN(DIRECTF)` on the same type succeeds) after the host compiler accepted the dereferenced form and Apple's own compiler still didn't. UCSD's own source already avoids the sugar entirely, calling `FWRITELN`/`FWRITESTRING` directly as ordinary forward-declared procedures (finding 136) |
| `2026-08-29-pascalsystem-printerror` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `PRINTERROR` (segment 3) written for real | **0 errors**, 474 lines; **exact match** to Apple's real binary (`params=4/data=46`). Not a straight UCSD port -- Apple's real `CASE` arms rework several messages, add a 16th `XEQERR` arm and three `IORSLT` arms for 128K ProFile support, drop one -- written directly from the real binary's own text. `S: STRING[45]` (not UCSD's `STRING[40]`) closed the frame exactly, matching the real `SINSERT` call's own `DESTLENG=45`. Surfaced a second calling-convention restriction distinct from finding 136's: a literal cannot bind to a `VAR` parameter through a *direct* call (`SINSERT('...', ...)` is error 154) even though Apple's own compiled code shows the same literal reaching that same parameter -- legal there only as part of the compiler's own lowering of ordinary `INSERT` sugar, which is what this file now uses instead (finding 137) |
| `2026-08-29-pascalsystem-string-primitives` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `SCONCAT`/`SINSERT`/`SCOPY`/`SDELETE`/`SPOS` (segment 0, what `CONCAT`/`INSERT`/`COPY`/`DELETE`/`POS` sugar lowers to) written for real | **0 errors**, 615 lines; **all five exact** against Apple's real binary. Surfaced a real, independent finding: Apple's actual declared parameter *order* for these five is not UCSD's own literal `GLOBALS.TEXT` order -- recovered by reading each real body's own algorithm directly (which word is read from vs. written to vs. compared as a plain `INTEGER`), not by assuming a "reverse the list" shortcut, which fit some of the five and not others (`SPOS` needs no reordering at all). One real miss caught and closed: `SPOS`'s own local buffer, first sized `STRING[72]` on a double-subtraction of the params/result/gap that the lift's own "locals N words" figure already excludes, corrected to a plain default `STRING` (finding 138) |
| `2026-08-29-pascalsystem-syscomrec` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with the real `SYSCOMREC` type added and `PRINTERROR`'s own placeholder global fixed | **0 errors**, 686 lines; `PRINTERROR` still exact after the change. First real progress on `PASCALSY`'s own 451-word global `VAR` section: told apart two distinct addressing modes (`G<n>` = a segment-0 flat procedure's own params/locals, or a `SEGMENT PROCEDURE`'s absolute global reference, depending which kind of procedure; `I1,n` = a flat procedure's lex-relative reach into `PASCALSY`'s own globals), then confirmed `SYSCOM` (offset 1) and `GFILES[1]` (offset 3, not a separate `OUTPUTFIB`) by reading `FGOTOXY`'s own real body directly against the newly-added `SYSCOMREC.CRTINFO.WIDTH`/`HEIGHT` fields (finding 139) |
| `2026-08-29-pascalsystem-fgotoxy` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `FGOTOXY` (segment 0) written for real | **0 errors**, 719 lines; `params=4/data=0` against Apple's real `4/2` -- one word short, exactly the gap predicted before compiling (Apple's own body caches `SYSCOM+37` in a local rather than re-reaching `SYSCOM^.CRTINFO` twice), documented not forced. First real payoff of finding 139's `SYSCOM`/`GFILES[1]` work (finding 140) |
| `2026-08-29-pascalsystem-maxunit` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `MAXUNIT` corrected from UCSD II.0's own `12` to `20` | **0 errors**; `PASCALSYS.CODE` extracted and diffed procedure-by-procedure against Apple's real `128K.PASCAL` via `CodeFile` -- every one of segment 0's 42-of-43 `params` matches held unchanged (the change only touches global `data`, never a parameter list), and procedure 1's own `data` (the outer block carrying the global `VAR` section) grew from 526 to 622 bytes -- 263 to 311 of the 451 words needed, matching the host compiler's independent figure exactly. Neil Parker's documented `DISKNUM` table settled it: "12 (Apple Pascal 1.1) or 20 (Apple Pascal 1.2 and 1.3)" -- raised directly by the user, not found independently (finding 141) |
| `2026-08-29-pascalsystem-maxseg` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `MAX_SEG` corrected from UCSD II.0's own `31` to `63` | **0 errors**, 729 lines; `PASCALSYS.CODE` extracted and diffed procedure-by-procedure against Apple's real `128K.PASCAL` -- `params` match held for 1-42, and procedure 1's `data` is **unchanged** at 622 bytes, confirmed before compiling: `MAX_SEG` only widens `SYSCOMREC.SEGTABLE`, reached through the `SYSCOM` pointer, never a field of the outer block's own `VAR` section. Same source as finding 141 -- Neil Parker's own annotation on `GLOBALS.TEXT`'s literal `MAX_SEG = 31`: "31 for the 64K system, 63 for the 128K system" -- again raised directly by the user (finding 142) |
| `2026-08-29-pascalsystem-varsection-complete` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with the ~26 Apple-added globals past `FILENAME` written for real (Dave Tribby's names via Neil Parker's transcription) | **0 errors**, 780 lines; `PASCALSYS.CODE` extracted and diffed procedure-by-procedure -- **procedure 1 (`PASCALSY.1`) `params=0/data=902`, an exact match** to Apple's real `128K.PASCAL`, first attempt, both the host compiler and Apple's own agreeing. `params` match held for 1-42 elsewhere. Parker's own offset list checked out two independent ways before a line was typed: every gap between his offsets matches this file's own word-size formulas exactly across ~26 fields, and the block's own starting offset landed exactly on this file's prior Apple-verified total through `FILENAME` (622 bytes). Field boundaries are now VERIFIED BINARY FACT (a wrong split anywhere would have changed the frame total); individual field names/types stay STRONG INFERENCE -- six of Tribby's own fields are unidentified placeholders (`WHAT_F`-`WHAT_L`) (finding 143) |
| `2026-08-29-pascalsystem-mainloop-start` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `PASCALSY.1`'s own real body started (`EMPTYHEAP := NIL`, `UNITCLEAR(1)`, `INITIALIZE`) | **0 errors**, 839 lines; procedure 1 still `params=0/data=902`, exact, unchanged (none of the three added lines need a local). Two pieces of the real body left out and documented rather than guessed: the `FINIT`/`FCLOSE` calls on a file variable this file hasn't correctly identified yet (`LLA 696` is dead `VADDR+FILESIZE` arithmetic already proven harmless by findings 43a/118c, so only `@L396` itself matters), and the `REPEAT` loop's own `PASCALSY.48` call, which needs procedures 44-58 written first -- genuinely new territory past the 41/42 forward-declared set (finding 144) |
| `2026-08-29-pascalsystem-execfile-finit` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with seven of finding 143's own Tribby-guessed fields (`WHAT_I`-`WHAT_K`) collapsed into one real `EXEC_FILE: FILE` | **0 errors**, 862 lines; procedure 1 still `params=0/data=902`, exact. The `FINIT`/`FCLOSE` calls neither this file nor its author wrote a line of source for -- Apple's own compiler auto-generates both from the `VAR EXEC_FILE: FILE` declaration alone, and the resulting instruction stream matches Apple's real p-code **byte for byte**: entry `LLA 396 / LLA 696 / LDCI 1 / NGI / CXP 0,3`, exit `LLA 396 / SLDC 0 / CXP 0,6 / XIT`. Found via a silence test (`grep`ing the whole lifted disk set: no offset strictly inside the 40-word span between 396 and 435 is ever referenced anywhere) plus a size match (40 words = `NILFILESIZE` exactly) plus independent confirmation from two real `FWRITESTRING` calls in `PASCALSY.54` landing exactly on the next fields' own compiled offsets (finding 145) |
| `2026-08-29-pascalsystem-exec-44-47` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `PASCALSY.44`-`47` (the EXEC-file buffered I/O layer) written for real, plus two more of finding 143's own Tribby-guessed fields corrected (`WHAT_F` -> two pointers, `WHAT_H` -> two booleans) | **0 errors**, 1000 lines; all four **exact** against Apple's real `128K.PASCAL` (`44: 2/0`, `45: 2/0`, `46: 0/0`, `47: 0/0`), procedure 1 still `902`/`902`. `FCLOSE`/`FBLOCKIO` can't be called directly on `EXEC_FILE` (type-checking rejects a raw `FILE` where `VAR F: FIB` is declared) -- fixed via the standard `CLOSE`/`BLOCKREAD`/`BLOCKWRITE` sugar, precedent already in this project's own verified `PASCALCO.text`. `SCAN`'s real 3-argument form (`SCAN(-511, =CHR(13), EXBUFPTR^[511])`), not the lift's raw 6-operand dump, matching finding 138b's own `SPOS` precedent. `46`/`47` compile to Apple's real `data=0` -- reused the existing global `PL: STRING` for scratch error-message text rather than adding a local, since neither procedure has any local budget at all (findings 146/147) |
| `2026-08-30-pascalsystem-waitsysvol` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `PASCALSY.50` (`WAITSYSVOL`) written for real, `PASCALSY.48`/`49` reserved as number-preserving stubs (`STUB48`/`STUB49`) | **0 errors**, 1073 lines; procedure 1 still `902`/`902`, `44`-`47` still exact. Procedure 50's `params=0` matches exactly; `data` is documented short by one word (1 vs Apple's real 2) rather than forced -- likely tied to a third open gap, `GFILES[1]^` compiling to runtime array-index code here (matching the already-verified `EXECREADBLK`/`EXECWRITEBLK` shape) where Apple's real binary uses one direct `LOD 1,3` instead, suggesting the real source reaches that word through a different identifier not yet placed. Three more global offsets confirmed by probe: `SYSCOM^.SYSUNIT` is field 3 not field 2, `SYVID`/`DKVID` = words 63/59 (not the other way around, per finding 93a's within-group descending rule), `STATE`/`SWAP_ON` = words 69/388 (finding 148) |
| `2026-09-02-pascalsystem-write-sugar` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with all console output rewritten as Apple wrote it -- bare `WRITE`/`WRITELN`, **no file argument** -- 103 call sites converted, 28 scratch-`STRING` pairs and 7 dead declarations removed, and `BADTITLE`'s parameter changed from `VAR TITLE: STRING` to a value `STRING` | **0 errors**, 4096 lines. Scored with the new `tools/oscmp.py` against shipped `128K.PASCAL`: **15 of 111 procedures instruction- *and* frame-identical, up from 10, none lost** -- `GETCMD.6`, `GETCMD.23`, `PASCALSY.51`, `PASCALSY.52`, `PRINTERR.1` gained, `PASCALSY.2` (`EXECERROR`) closed completely. Proved without an emulator run first, from this project's own byte-identical `FORMATTER.text`/`FORMATTER.CODE` pair, which contains the identical construct and the identical `LOD 1,3 \| LSA \| SLDC 0 \| CXP 0,19`; that also retires the two-session-old "`LOD 1,3` is some unrecovered identifier" note, since it is the compiler's own default-output reference and has no identifier in the source. Wired into `build_all.py` as `probes/probe_os_exact.py`, which names all fifteen and keeps four known-differing procedures as a discrimination control (finding 201) |
| `2026-09-02-pascalsystem-param-order` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with the four string primitives' parameter lists put back to UCSD's own `GLOBALS.TEXT` order (finding 138 had written each real *frame* out as the declaration, which is the reverse of what produces it), all nine call sites reordered to match, `SINSERT`'s `ONRIGHT := 0` guard restored, `RUNWORKFILE` rewritten as UCSD's own `ASSOCIATE(CONCAT(CODEVID,':',CODETID), ...)` expression, and `GETYESNO`'s console read written as a bare `READ(CH)` | **0 errors**, 4131 lines. **20 of 111 procedures instruction- *and* frame-identical, up from 15, none lost.** `PASCALSY.23`/`.25`/`.26` closed on the declaration change alone, `.24` after the guard went back, `GETCMD.4` on the `READ` sugar. `GETCMD.2` went from 58 instructions and `data=166` to Apple's own 54 in Apple's own order at `data=26` -- one operand short of exact, and that one is a genuine open question (Apple passes `LAO 6`, a global, where this passes a local; see finding 202b). Probe floor raised to 20 in `probes/probe_os_exact.py` (finding 202) |
| `2026-09-02-pascalsystem-execblk` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with Tribby's `what_g` split into two scalars (the real binary never indexes it) and `EXEC_BLK` re-typed `BOOLEAN`, stepped with `SUCC` and read as a block number through `ORD` | **0 errors**, 4137 lines. **24 of 111 procedures instruction- *and* frame-identical, up from 20, none lost** -- the whole EXEC-file buffered I/O layer, `PASCALSY.44`/`.45`/`.46`/`.47`, closed in one compile. The direction was settled by a *failed* run first: declaring `EXEC_BLK` `INTEGER` and writing `IF ... AND EXEC_BLK` is **error 134** on Apple's own compiler, which ruled out the reading that had looked obvious and left `SUCC`/`ORD` -- the two conversions that emit no instruction at all -- as the only way one word can be a BOOLEAN operand, a BOOLEAN condition, an addend and `FBLOCKIO`'s `RBLOCK` at once (finding 202d) |
| `2026-09-02-pascalsystem-keyboard` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `WAITSYSVOL` and `SWAPMENU` reading through **`KEYBOARD`** (Apple Pascal's third predeclared file, global 4) instead of `GFILES[2]^`, `INSERT`/`EOLN` written as sugar rather than direct `SINSERT`/`FEOLN` calls, `SWAPMENU`'s two locals put in the binary's own order, and `WAITSYSVOL`'s unreferenced leading word declared | **0 errors**, 4162 lines. **26 of 111 procedures instruction- *and* frame-identical, up from 24, none lost** (`PASCALSY.50`, `GETCMD.26`). `READ(KEYBOARD, ...)` reproduces the real binary's bare `LOD 1,4`/`LOD 2,4` exactly, where `GFILES[2]^` had cost the four-instruction index expansion; `INSERT(...)` emits `CXP 0,24` where the hand-written `SINSERT` call emitted `CBP 24`, which is what tells sugar from an ordinary call in p-code. A check settled that `GFILES` really is still an array, unlike `WHAT_G`: inside `PASCALSY` it is indexed in exactly one procedure with exactly the constants 0 and 1 (finding 202e) |
| `2026-09-02-pascalsystem-putcrt` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `PASCALSY.53`-`.58` put in Apple's own numbering -- `PUTCRT` written for real at `.53`, a five-word `STUB54`, `BLKXFER` moved inside `FBLOCKIO`'s body to reach `.55`, `FGET`'s completion moved down past it with a nested `STUB56` -- and `HOMECURSOR`/`CLEARSCREEN`/`CLEARLINE` rewritten to call `PUTCRT` instead of inlining it | **0 errors**, 4175 lines. **31 of 111 procedures instruction- *and* frame-identical, up from 26, none lost** (`PASCALSY.36`, `.37`, `.38`, `.53`, `.55`). The numbering came from the call graph rather than guesswork: `CLP` means "nested inside me", so `.55` is `FBLOCKIO`'s, `.56` is `FGET`'s and `.58` is `STUB48`'s, which is why `GETNEXTCMD`/`CMDDISPATCH` belong on `57`/`58` and not the `54`/`55` this file had them on. The note claiming a shared helper could not be factored out without shifting later numbers was answerable all along. Inlining had also had the `PREFIXED` test **backwards** -- the binary has `LDP` then a bare `FJP`, no `LNOT` (finding 202f) |
| `2026-09-02-pascalsystem-newtags` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `INITHEAP`'s six FIB allocations given their variant tags (`NEW(p, TRUE, FALSE)`), and `STUB48`/`CMDDISPATCH`'s loops written as the `REPEAT ... UNTIL` the binary tests at the bottom rather than `WHILE`, with `CMDDISPATCH`'s `ELSE` arm a plain `EXIT` | **0 errors**, 4168 lines. **33 of 111 procedures instruction- *and* frame-identical, up from 31, none lost** (`INITIALI.8`, `PASCALSY.48`). A plain `NEW(p)` allocates `FIB`'s largest variant, 290 words, because of the 512-byte soft buffer; `NEW(p, TRUE, FALSE)` stops at `FISOPEN=TRUE, FSOFTBUF=FALSE` and the field-by-field count comes to Apple's `SLDC 30` exactly. The same run **localised the `FIOPRIMS` blocker**: `PASCALSY.58` is now identical apart from six compiler-emitted instructions, a `LOADSEGMENT(2)`/`UNLOADSEGMENT(2)` residency wrapper on the intrinsic unit's own segment -- a far sharper handle on finding 200's open item than "`FGET` is a stub" (finding 202g) |
| `2026-09-02-pascalsystem-resident` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with the `$R` "resident" option applied where the real binary carries a `LOADSEGMENT`/`UNLOADSEGMENT` wrapper: `(*$R FIOPRIMS*)` in `CMDDISPATCH`, `(*$R FILEPROC*)` in `GETNEXTCMD`, `(*$R 6*)` in `INITIALIZE` | **0 errors**. **34 of 111 procedures instruction- *and* frame-identical, up from 33, none lost** -- `PASCALSY.58`, the last non-stub procedure in the segment-0 command loop. All three procedures now carry Apple's exact six-instruction wrapper. The construct was already in this repository: `SYSTEM.COMPILER`'s byte-identical `BODYPART` has `BEGIN (*$R ROUTINE*) BODY END` in `HOLDRTN` and `(*$R STATEMENT*)` in `HOLDSTMT`, producing the same shape against Apple's own bytes; the manual then confirms both the identifier and number forms. `INITIALIZE` must use the number -- naming `FILEPROC`, which is declared later because segment numbers follow declaration order, is error 273 (finding 203) |
| `2026-09-02-pascalsystem-cmdstate` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `CMDSTATE` given its real **eleventh** member and `GETNEXTCMD` rewritten around the `CASE` the binary dispatches with | **0 errors**. **35 of 111 procedures instruction- *and* frame-identical, up from 34, none lost** (`PASCALSY.57`). `XJP 2..10` and the set constant `2044` (bits 2-10) both reach an ordinal `CMDSTATE` did not have; the other set constants (`224` = bits 5-7, `12` = bits 2-3) prove the extra member is appended, not inserted, since every existing name keeps its ordinal. Five things about `GETNEXTCMD` were wrong besides: a `CASE` not an ordinal comparison, `REPEAT` not `WHILE`, two inverted conditions, `EXIT` not `GOTO`, and a discarded-result call plus a real `IORESULT` local where this file had a `DUMMY`. **The whole segment-0 command loop is now byte-exact end to end** (finding 204) |
| `2026-09-02-pascalsystem-associate` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `GETCMD.19` (`ASSOCIATE`) written for real -- 259 instructions, `params`/`data` `16`/`728` -- plus `GETCMD`'s own three-word `VAR` block and the `SEGDICT` type its dictionary reads through | **0 errors**, 4388 lines. **37 of 111 procedures instruction- *and* frame-identical, up from 35, none lost** (`GETCMD.19`, and `GETCMD.2` closed by the same `DONT_CARE` the segment `VAR` block gave it). The sixth parameter is a BOOLEAN, not the `INTEGER` this file guessed. Global word `444` is `RUN_VOL`, the `%` volume -- declared in this file all along, at the right offset, with its own comment beside it, while both readers wrote `DKVID`; the one wrong offset is a byte shorter to encode and flipped the parity of every alignment `NOP` after it, so a single error read as thirteen. The four-word set at `0x120` of the segment dictionary is the intrinsic units a codefile requires: `128K.PASCAL`'s own is `0004`, bit 2, and segment 2 is `FIOPRIMS` (finding 205) |
| `2026-09-03-pascalsystem-scantitle` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `PASCALSY.33` (`SCANTITLE`) rewritten to normalize the value `STRING` parameter's own compiler-made copy in place, with `FTITLE[I]` cached in a `CHAR`, the upshift written as `ORD(CH) - ORD('a') + ORD('A')`, two integers instead of three and the digit scan as a `REPEAT` | **0 errors**, 4412 lines. **38 of 111 procedures instruction- *and* frame-identical, up from 37, none lost.** The `data` gap had been 82 bytes -- 41 words, one `STRING`, exactly -- for four sessions, and it was a local `T` that should never have existed: a value `STRING` parameter is a scratch buffer the caller already paid for, and Apple's OS writes to it. 354 instructions, the second-largest procedure in the segment (finding 206) |
| `2026-09-03-pascalsystem-fileprims` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with the console file primitives written for real -- `FREADCHAR` (`.16`), `FWRITECHAR` (`.17`), `FWRITEBYTES` (`.20`), `CHECKDEL` (`.54`, which had been a five-word stub), `EXECGETCH` (`.56`) and `FGET`'s own frame -- plus the four-line `REPEAT` the outer block was missing | **0 errors**, 4690 lines. **44 of 111 procedures instruction- *and* frame-identical, up from 38, none lost.** `SRO 11` from a lex-1 procedure was the blocker and it is settled: `BASE` is the *lex-0* activation record, not the globals, checked against 1123 BASE-relative references across the whole file with zero outside the referencing procedure's own frame. `CHECKDEL` placed three packed-field offsets straight out of finding 93a's reversal rule -- `CRTCTRL` at `SYSCOM+31`, `ERASEEOL` as the high byte of its word 1, `CRTINFO.CHARDEL`/`LINEDEL` at `+43`/`+44` low -- with nothing left over. `FWRITESTRING` (`.19`) is deliberately left open: its `FWRITEBYTES` argument is `S+1` and Apple's own compiler refused both readings tried, **error 103** for `S[1]` and **error 142** for `S`, which is real negative evidence rather than a third guess (findings 207-209) |
| `2026-09-05-pascalsystem-getcmd-libs` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `GETCMD.11` and its whole nested family (`.13`-`.18`) written for real | **0 errors**. **95 of 111**, seven procedures in one compile. They had to land together: the children carry almost no frame of their own and reach into `.11`'s with `LDA 1,<n>`, so `.11`'s layout was mapped from its children's operands and neither half was evidence on its own (finding 227) |
| `2026-09-06-pascalsystem-getcmd-complete` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with the EXEC-file pair, `STARTCOMPILE` (`.20`/`.21`) and the segment body `.1` | **0 errors**. **101 of 111**, and **`GETCMD` is complete, all 27**. `.1` went last on purpose -- every `CLP` in it is a procedure number, and a wrong one compiles as quietly as a right one until all 26 callees are real. Two one-instruction misses taught the same rule: within-clause reversal reaches record fields and packed bits, so `INC 2` off a `FIBP` is `FEOF` and `MISCINFO` bit 4 is `SLOWTERM` (finding 228) |
| `2026-09-06-pascalsystem-fileproc` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `FPTITLE` (`FILEPROC.8`) rewritten whole and `FPOPEN`'s (`FILEPROC.4`) frame closed | **0 errors**. **104 of 111**. `POS`/`COPY`/`DELETE`/`CONCAT` are sugar and bypass the VAR-actual check the same way `WRITE` does, which was worth a whole procedure; and a `WITH` pointer's slot number counts how many `WITH`s are open at that point, so a wrong slot means a wrong *extent*, not a missing `WITH` (finding 229) |
| `2026-09-06-pascalsystem-odd` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `ODD(...)` written at the four finding-218b sites and `FIOPRIMS.5` (`FPWINOUT`, 275 instructions) written from scratch | **0 errors**, 6538 lines. **108 of 111, up from 104, none lost** -- `FIOPRIMS.4`, `FIOPRIMS.5`, `FILEPROC.2`, `FILEPROC.4`. **`FILEPROC` is complete, all 8.** `ODD` and `CHR` are the two standard functions `CALLNONSPECIAL` emits no `CSP` for, so `ODD(<integer>)` is a free cast to `BOOLEAN` -- which is exactly what the four procedures finding 230 had declared permanently unreachable were each two instructions short of. `FPWINOUT` landed exact on the first compile, its frame and local numbering read off `FPWINADV`'s identical first twelve instructions (finding 231) |
| `2026-09-06-pascalsystem-unit` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with `FIOPRIMS` rewritten as an inline `UNIT`, `USERPROGRAM` forward-declared around it, and `FPUT` written for real and moved to the head of the 41 completions | **0 errors**, 6570 lines. **110 of 111, up from 108, none lost** -- `FIOPRIMS.1` and `PASCALSY.8`. **`FIOPRIMS` is complete, all 5.** No library round trip was ever needed: `GETTEXT` searches `MODPTR` before it opens a library and reads an inline unit's interface text back out of the work codefile. Non-intrinsic on purpose -- `INTRINSIC CODE 2` does not advance `NEXTSEG`, so `PRINTERROR` would claim 2 as well. The trap: a `USES` re-parses the interface with `NEXTPROC := 2` and so zeroes `PROCTABLE[2..5]` of the current segment, which emptied `EXECERROR`/`FINIT`/`FRESET`/`FOPEN` on the first attempt; `FPUT` going first among the completions is the fix, and is also why `FGET` cannot follow (finding 232) |
| `2026-09-06-pascalsystem-fget` | `SYSTEM.COMPILER` 1.3 on the same `PASCALSYSTEM.text` with `FGET`'s real body and its own `USES FIOPRIMS;` -- the source that produced it is kept alongside, so the run is reproducible rather than merely archived | **0 errors**, 6668 lines. **`PASCALSY.7` (`FGET`) instruction- and frame-identical, 234 instructions, first attempt** -- and `PASCALSY.2`-`.5` emptied, those four and no others, which is finding 232b measured instead of argued: a `USES` re-parses the interface with `NEXTPROC := 2` and zeroes `PROCTABLE[2..5]`, and `FGET`'s completion cannot move ahead of `EXECERROR`'s because its nested `EXECGETCH` must claim procedure 56. With the `-unit` run above, **all 111 of `128K.PASCAL`'s procedures have now been produced byte-for-byte by Apple's own compiler**; no single compile holds them at once, and `probe_os_exact.py` checks the union (finding 233) |
| `2026-09-07-assembler-skeleton` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` -- the `SYSTEM.ASSMBLER` reconstruction's first compile: `(*$U-*)` with `TLA` as a `SEGMENT PROCEDURE` and `(*$NS 7*)`, the 2215-word `VAR` block, 21 `FORWARD`s, five nested segment procedures and every body a stub except `TLA.1`. The source that produced it is kept alongside | **0 errors**, 672 lines. **Six segments with Apple's names, numbers, `SEGKIND` and procedure counts** -- `TLA` 1, `INITIALI` 7, `SYMTBLDU` 8, `PROCEND` 9, `ASSEMBLE` 10, `PRINTERR` 11 -- and **all 90 shared procedures agreeing on lex level and `PARAM SIZE`**, not one disagreement. `TLA.1` **instruction- and frame-identical**, `params`/`data` 4/4430, which is the whole `VAR` block and the file declaration order measured at once. Two differences and they are named: Apple's `PASCALIO` cannot be rebuilt (finding 235b), and the `PASCALSY` host segment is finding 105a showing up in a second file (finding 237) |
| `2026-09-07-assembler-bodies` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with the six `FUNCTION` declarations corrected and five bodies written -- `TLA.21`, `ASSEMBLE.16`, `.18`, `.19`, `.20` -- plus `(*$I+*)`, global 10 as a one-word variant and globals 406 and 447 as `STRING[80]` | **0 errors**, 701 lines. **6 of 95 instruction- and frame-identical, up from 1, none lost**, all five on the first compile. A function's result costs two words, so `FUNCTION F: BOOLEAN` and `PROCEDURE P(X, Y: INTEGER)` are the same PARAM SIZE and four of these were declared the wrong way while matching Apple's number exactly -- `RNP 1` is what tells them apart, and Apple's compiler rejects a `FUNCTION` completed as a `PROCEDURE` with **error 160** where the fast tier accepts it. `$U-` turns IOCHECK off and Apple turned it back on: 184 `CSP 0`s (finding 238) |
| `2026-09-07-assembler-record` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with global 3 given the record it points at, globals 12 and 68 as `BOOLEAN`, `T18` as a `BOOLEAN` function, and five more bodies -- `ASSEMBLE.17`, `.21`, `.29`, `.32` and `TLA.31` | **0 errors**, 746 lines. **11 of 95 instruction- and frame-identical, up from 6, none lost.** A RECORD's field list reverses within a clause exactly as a VAR block's does: `A4, A5, A6, A7, A8: INTEGER` put A5 on word 7, one instruction wrong in an otherwise identical 25-instruction `CASE`, while A6 landed right by coincidence and two other procedures using it came out exact in the same run. Global 3 is `NEW`'d at 7, 8 and 9 words, so the real record is a variant (finding 239) |
| `2026-09-07-assembler-dispatch` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `ASSEMBLE.17`'s whole nested family written -- the 6502 opcode dispatcher and all fourteen of its arms -- plus a whole-word variant on global 10, global 622 as a record and global 685 as `ARRAY [0..5]` | **0 errors**, 967 lines. **20 of 95 instruction- and frame-identical, up from 11, none lost**; `ASSEMBLE` is 17 of 33. Six arms share a four-instruction prologue and five share a tail, so once the first was right the rest were transcription. `A24` is not an arm at all -- it is nested inside `A23` -- and reading the jump table as arms-in-procedure-order would have put every arm after the fourth one procedure too far along (finding 240) |
| `2026-09-07-assembler-const` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `TLA.14`'s frame mapped from its children's operands -- two `BOOLEAN` parameters and two byte-pair locals -- and `TLA.27`/`TLA.28` written, plus a declared `CONST` of -1 | **0 errors**, 1020 lines. **22 of 95 instruction- and frame-identical, up from 20, none lost**, and `TLA.14`'s own frame reaches Apple's 6/10 with a stub body. `GENLDC` emits one byte for 0..127 and two-byte `LDCI` otherwise, so `-1` written in an expression gives `SLDC 1 | NGI` while a declared `CONST` of -1 gives `LDCI 1 | NGI` -- one byte, and the whole evidence that Apple named a constant there (finding 241) |
| `2026-09-07-assembler-printerror` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PRINTERROR` written whole | **0 errors**, 1073 lines. **23 of 95, up from 22, none lost, and `PRINTERR` is complete** -- its one procedure, `params`/`data` 2/728. `SEEK` is a standard procedure (`GENNR(31,2)` in `ROUTINE`), so the `USES PASCALIO` question findings 235b and 240 left open does not arise; trying it anyway gets **error 104** on the interface's own first line, since re-parsing it needs a `FIB` this program has no reason to declare. The trailing `CLOSE` is `BODY3`'s generated sweep, not source: writing one produced a checked call *and* the generated one (finding 242) |
| `2026-09-07-assembler-procend` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PROCEND.5` and `.6` written and `PROCEND.1`'s frame placed from its children's operands | **0 errors**, 1132 lines. **25 of 95, up from 23, none lost**, both exact on the first compile, and `PROCEND.1`'s own `data` reaches Apple's 1304 with a stub body. `LDA 3,343` and `LDA 2,343` from three children put a `FILE` at local 343 of a 652-word frame; its record is `KEY` then seven words, the same seven that are the type of the four globals `PROCEND.5` passes by `VAR`. Three `FOR` loops share two limit temps, the third reusing the first's (finding 243) |
| `2026-09-07-assembler-lientry` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with the eight-word record modelled as the variant it is -- UCSD's `LIENTRY` -- and `PROCEND.8` written | **0 errors**, 1207 lines. **26 of 95, up from 25, none lost.** `PROCEND.7` writes word 4 with 2, 3, 4, 5, 6, 11 and 12, exactly the `LITYPES` ordinals in the compiler's own `WRITELIN.text`. A lone `BOOLEAN` in a `PACKED RECORD` is *not* packed -- it gets a whole word, since the `INTEGER` after it starts a new word regardless -- so the flag compiled to `SLDL 3` until the byte `ASSEMBLE.5` reads at bits 8..15 was declared beside it (finding 244) |
| `2026-09-07-assembler-liwriter` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PROCEND.7` written -- the routine that writes the linker-information records | **0 errors**, 1320 lines. **27 of 95, up from 26, none lost**; 267 instructions, the largest body so far. Global 966 is a 128-bucket hash table of symbol pointers and `A4` is the chain link, which turns three of `ASMREC`'s placeholder `INTEGER`s into the pointers they are. A `CASE`'s arms are emitted in SOURCE order, not label order -- ascending labels put every arm one place wrong, and reordering the seven lines was the whole fix (finding 245) |
| `2026-09-07-assembler-procend2` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PROCEND.2` and `.9` written | **0 errors**, 1366 lines. **29 of 95, up from 27, none lost**; `PROCEND` is 6 of 9. `.2` exact first time; `.9` took three one-instruction fixes with three different causes -- `WORKCODE` not `WORKSYM` (one clause reverses), the declared `MINUS1` rather than a written `-1`, and `(*$I-*)` around a `BLOCKWRITE` whose result is tested. `NEW`'s own `LDCI 256` is what fixes the code buffer at one disk block (finding 246) |
| `2026-09-07-assembler-blockio` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PROCEND.4` written and `.9`'s `BLOCKWRITE` corrected to three arguments | **0 errors**, 1418 lines. **30 of 95, up from 29, none lost**; `PROCEND` is 7 of 9 and `.4` was exact first time at 180 instructions. `ROUTINE`'s `BLOCKIO` settles two things: mode 1 is a READ and mode 0 a WRITE (`LKEY = 37` is `BLOCKREAD`, by counting `ENTSPCPROCS`'s name list), and an omitted block number is `GENLDC(-1)` -- so `LDCI 1 | NGI` here is the compiler's own default, not the declared constant that produced the same bytes in `TLA.27` (finding 247) |
| `2026-09-07-assembler-procend3` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PROCEND.3` written and four globals typed | **0 errors**, 1452 lines. **31 of 95, up from 30, none lost**; `PROCEND` is 8 of 9. Global 64 turns out to be a `BOOLEAN`, which made `TLA.1`'s `ODD(G64)` -- added in the first session because the placeholder was an `INTEGER` -- **error 142**. Both spellings emit `LDO 64`, so the byte compare could never have caught it: a placeholder type can force a workaround that is invisible in the output and wrong in the source (finding 248) |
| `2026-09-07-assembler-procend4` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `PROCEND.1`, the segment's own body, written and its frame corrected to 651 declared words | **0 errors**, 1625 lines. **32 of 95, up from 31, none lost, and `PROCEND` is complete -- all nine, the first whole segment of the assembler to come out Apple's bytes.** 520 instructions, exact on the first compile. The skeleton's 652 declared words matched `data` 1304 only while the body was empty: the two `FOR` loops are sequential, so they share one limit temp and the declarations stop at 651. `BODY2`'s second `LDA` is `VADDR+FILESIZE` whatever the file is, so it is not a width -- the untyped `FILE` here gets `NILFILESIZE` = 40 words and its window address lands inside the next variable (finding 249) |
| `2026-09-07-assembler-pseudoops` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with seven of `ASSEMBLE`'s data-generating pseudo-op handlers written -- `.5`, `.7`, `.8`, `.11`, `.12`, `.13`, `.14` | **0 errors**, 1801 lines. **39 of 95, up from 32, none lost**; `ASSEMBLE` is 24 of 33. Six exact on the first compile; `.8` needed one fix, `G22 + (V2+1) DIV 2` written the way Apple wrote it, `(V2+1) DIV 2 + G22` -- addition commutes and the code generator does not. `ASSEMBLE.5` retired finding 249c's invented five-word record: it walks a chain of them and then `MOV 5`s one into the file window's `INFO`, which has been a `FIVEREC` since finding 244. `ODD` is this program's way of testing a low bit (finding 250) |
| `2026-09-11-assembler-asmrec` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with `ASMREC` written as the variant record it is, and four bodies from an outside review of this tree re-derived and re-verified -- `INITIALI.3`, `TLA.24`, `ASSEMBLE.9`, `ASSEMBLE.33` | **0 errors**, 1929 lines. **43 of 95, up from 39, none lost**; `ASSEMBLE` is 26 of 33. Word 7 of `ASMREC` is an `INTEGER` beside a `REFP` on `PROCEND`'s path and a `FIVEP` on `ASSEMBLE.9`'s, so the 7/8/9-word `NEW` sizes finding 239 recorded are its three arms. Apple's compiler accepts the empty variant `0: ()`; the host compiler rejects it, so the fast tier can no longer pre-check this file. `ASSEMBLE.33` arrived scored exact and was not -- `T17` one nesting level too deep emits the same 38 instructions in the same order and moves one `FJP` from the 35th to the 36th, which is finding 252 (findings 251-252) |
| `2026-09-11-pascalsystem-jumps` + `-fget` | `SYSTEM.COMPILER` 1.3 on `PASCALSYSTEM.text` with finding 252's three jump-target defects fixed -- `EXECERROR`'s CONTROL-RESET hang moved to the outer `IF`'s `ELSE`, `SCANTITLE`'s result assignment moved out of an `ELSE`, `BLKXFER`'s `LEN :=` moved into its `THEN` | **0 errors**, 6714 and 6698 lines. **110 of 111 in the unit run and 111 of 111 across both, all three fixed first attempt** -- and this time under a comparison that can see a jump go to the wrong place. `EXECERROR` had been hanging on exactly the errors Apple offers to continue from and rebooting on the ones Apple treats as fatal; `SCANTITLE` returned `FALSE` for any title with nothing after the file name. The `-fget` source is derived by `tools/fgetvariant.py` from the live file, not kept as a snapshot -- the old one was 13,369 diff lines stale (finding 253) |
| `2026-09-11-assembler-complete-segment` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with the last eight of `ASSEMBLE` written -- `.6`, `.4`, `.10`, `.2`, `.3`, `.15` and the segment body `.1` | **0 errors**, 2360 lines. **50 of 95, up from 43, none lost, and `ASSEMBLE` is complete -- all 33.** `ASSEMBLE.10` forced `ASMREC`'s word 6 into the variant part exactly as finding 251e predicted it would, which a tagless `CASE` makes free: every name stays reachable whatever was allocated. `.1` cost one instruction, `CXP 1,7` where this had `CLP 7` -- `TLA.7` and `ASSEMBLE.7` called twenty lines apart. `.2`'s `VAR` block after its nested function is error 6, and `A3` is a `BOOLEAN` the probe cannot distinguish from an `INTEGER` (finding 254) |
| `2026-09-11-assembler-symtbldump` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with the whole `SYMTBLDUMP` segment written -- the `.SYMBOLS` listing: a binary sort tree built from the 128-bucket hash chain, an in-order walk, and the 95-word frame its two children reach into | **0 errors**, 2520 lines. **53 of 95, up from 50, none lost, and `SYMTBLDU` is complete -- all three, first compile.** `WRITE` of a `PACKED ARRAY OF CHAR` takes a one-word `LOADADDRESS`, not a two-word `BYTEADDRESS`, which is the whole of why `FWRITEBYTES` shows three arguments after the file. Which tree child is which comes from the traversal order, not the insert (finding 255) |
| `2026-09-11-assembler-initialize` | `SYSTEM.COMPILER` 1.3 on `ASSMBLER.text` with the whole `INITIALIZE` segment written -- the 33-entry directive table, the opcode-file reader and its hash, the listing-file prompt, the version check | **0 errors**, 2857 lines. **58 of 95, up from 53, and five of the six segments are complete.** `INITIALI.2`'s table names every token code the file compares against and confirms every handler already written, with nothing revised. `.4`'s hash is bitwise arithmetic on a `BOOLEAN`-declared word -- `ODD(127)` is a mask. `NEW(p, n)` picks an arm only when the `CASE` has a named tag field. And a first attempt scored 55 rather than 58 because `(*$I-*)` crossed the segment boundary and cost `PROCEND.2`, `SYMTBLDU.1` and `.3` their `CSP 0`s: a net gain can hide a regression (finding 256) |
