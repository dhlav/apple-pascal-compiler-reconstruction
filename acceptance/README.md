# Acceptance runs

What Apple's own tools produced, kept as it came off the emulator.

Everything else in this repository is either evidence (Apple's shipped
media, never modified) or generated (`build/`, `analysis/`,
`reference_source/`, rebuilt by `tools/build_all.py`). This is neither. An
acceptance run happens inside AppleWin, driven by hand through
`tools/emucompile.ps1`, `tools/emuassemble.ps1` or `tools/emulink.ps1`, and its output would be
gone the next time `WORK2.dsk` is rewritten. So the output is kept here, and
a probe under `tools/probes/` compares it against the shipped binary on
every build -- which turns a one-off emulator session into a check that goes
on being run.

One directory per run: the codefile Apple's tool wrote, and the screen at
the moment it finished.

| run | what was run | result |
|---|---|---|
| `2026-08-24-formatter-native` | `SYSTEM.ASSMBLER` 1.3 on `src/native/FORMATTR.TEXT` (as `FMTNATIV.TEXT`) | 225 lines, **0 errors**; 354 bytes **identical** to `FORMATTER.CODE`'s procedure 2, relocation table included (finding 103e) |
| `2026-08-24-formatter-linked` | `SYSTEM.COMPILER` 1.3 on `FORMATTER.text`, then `SYSTEM.LINKER` joining it to the above | the whole codefile **identical** to `FORMATTER.CODE` -- dictionary and all 2672 bytes of segment `FORMATTE` -- to the end of the segment (finding 104) |
| `2026-08-25-compiler-search-asm` | `SYSTEM.ASSMBLER` 1.3 on `src/native/SEARCH.TEXT` | 519 lines, **0 errors**; the assembled `IDSEARCH`/`TREESEARCH` are what the linked run below copies in |
| `2026-08-25-compiler-linked` | `SYSTEM.COMPILER` 1.3 on the fifteen-segment `BODY13.TEXT` splice (before the body-order fix), then `SYSTEM.LINKER` joining it to `2026-08-25-compiler-search-asm` | superseded by the `-v2` run below; kept for the record. 14 of 15 segments byte-identical, both native routines exact; `PASCALCO` differed by an extra empty host segment and a different physical body order (finding 105) |
| `2026-08-25-compiler-linked-v2` | same, after `PASCALCO.text`'s bodies were reordered and `COMPILE` given a forward declaration | **all 15 real segments byte-identical**, `PASCALCO` included (finding 107). Not wired into `probe_acceptance.py`: the codefile as a whole still carries the extra empty `PASCALSY` segment (finding 105a), so a whole-file compare cannot pass yet even though every real segment does |
| `2026-08-27-libmap-showone-getword` | `SYSTEM.COMPILER` 1.3 on `LIBMAP.text` (as `LIBMAPT.TEXT`, compile-only, not linked) | **0 errors**; procedure 8 (`SHOWONE`) `params=2/data=556` and procedure 10 (`GETWORD`) `params=2/data=2`, both **exact matches** to Apple's shipped `LIBMAP.CODE` (finding 111 update). Not wired into `probe_acceptance.py`: `LIBMAP.text` is still mid-reconstruction (`SHOWINFO`/`SHOWREF`/`MAPLIBRARY`/outer block are stubs), so no whole-file or whole-segment compare is possible yet -- this run is a frame-size check on two procedures, not a byte compare |
| `2026-08-27-set40cols-compile` | `SYSTEM.COMPILER` 1.3 on new source `SET40COLS.text` (as `SET40T.TEXT`, compile-only, not linked -- no native half, nothing to link) | **0 errors, first attempt**; all four procedures' `params`/`data` exact matches to Apple's shipped `SET40COLS.CODE`; **2544 of 2560 bytes byte-identical**, all 16 real differences the same SEGINFO version-stamp byte (`$42` vs `$C2`, i.e. version 2 vs version 6) finding 99c already found unexplained in `BINDER.CODE` -- confirms the same mystery, not a new gap (finding 112) |
| `2026-08-27-library-compile` | `SYSTEM.COMPILER` 1.3 on new source `LIBRARY.text` (as `LIBRARYT.TEXT`, compile-only, not linked -- no native half) | **0 errors** after several real fixes (reserved word `INTERFACE`, `NEW`'s variant-tag-not-flex-size semantics, `FILLCHAR`'s 3-argument syntax, and getting `COPYSLOT`/`GETCOMMAND`'s nesting to match the binary's own lex levels). All 16 procedures present with the right structure; `SWAPBYTES`/`CHECKIO`/`GETINPUT` frames exact, several more close within a few words (documented, not forced), the copy/link/interface logic (procedures 11-15) deliberately left `(*STUB*)` -- this is a checkpoint, not a finished reconstruction (finding 113) |
| `2026-08-28-selfhost-library` | This project's own **reconstructed** `SYSTEM.COMPILER` (`acceptance/2026-08-25-compiler-linked-v2/COMPLINK.CODE`, swapped onto `SYSHD:` in place of Apple's shipped one) compiling the same `LIBRARY.text`/`LIBRARYT.TEXT` as the row above | **Byte-identical output, all 3072 bytes, 0 differences** against the same source compiled by Apple's own shipped `SYSTEM.COMPILER` -- the first real, substantial (not a calibration program) test of the reconstructed compiler actually compiling something end to end. Confirms finding 113's `MAINLOOP` frame-size anomaly is not a bug in this project's `SYSTEM.COMPILER` reconstruction -- both compilers agree exactly -- so whatever explains it is a difference between this project's `LIBRARY.text` and Apple's true original source, not a compiler quirk to keep chasing (finding 114) |
| `2026-08-28-library-copyslot` | `SYSTEM.COMPILER` 1.3 on `LIBRARY.text` with `COPYSLOT` (procedure 11) written for real (heap-chained `MARK`/`NEW`/`RELEASE` buffer, `WITH INDICT.ADDRLEN[FROMSLOT] DO` in place of the disassembly's illegal `@`, `BLOCKREAD`/`BLOCKWRITE` through it, the two-dictionary field copies, and the `SEGKIND NOT IN [0,6,7]` gate on `COPYLINK`) | **0 errors, first attempt**; `COPYSLOT` now matches Apple's binary exactly on **both** `params=4` and `data=10` -- the `params` mismatch (this project had 6, from a `VAR ABORT: BOOLEAN` parameter) resolved by discovering `ABORT` isn't a parameter at all: the disassembly's own `I1,3 := 0` writes directly into `MAINLOOP`'s frame, meaning `COPYSLOT` takes only `(TOSLOT, FROMSLOT: INTEGER)` and reads/writes `MAINLOOP`'s `ABORT` by lexical scoping, same sharing pattern LIBMAP's `GETWORD`/`SHOWONE` already established (finding 115) |
| `2026-08-28-library-copylink` | `SYSTEM.COMPILER` 1.3 on `LIBRARY.text` with `COPYLINK`/`READLINK`/`COPYINTERFACE` (procedures 12-14) written for real | **0 errors, first attempt**, 839 lines; all four remaining stubbed procedures (`COPYLINK`, `READLINK`, `COPYINTERFACE`, `IFACEWERR`) now match Apple's binary exactly on both `params` and `data` -- `COPYLINK`'s `data=534` (a whole 256-word link-info block plus its own current-entry register) and `COPYINTERFACE`'s `data=6` landed exact on the first attempt once `COPYINTERFACE` was written to reuse `COPYSLOT`'s own `MARKPTR` local (finding 116) rather than declare its own |
| `2026-08-28-binder-compile` | `SYSTEM.COMPILER` 1.3 on new source `BINDER.text` (as `BINDERT.TEXT`, compile-only, not linked -- no native half) | **0 errors** after three real fixes (a value STRING parameter's own 41-word copy-on-entry, `NEW` without a variant tag on a plain array, an 8-character identifier collision) and two frame-size corrections found via the acceptance tier (a spurious explicit string-copy local, a spurious second `STRING[80]` local where the real source reuses the first). Five of the six procedures now match Apple's shipped binary exactly on both `params` and `data`; the sixth (the outer program body) is short by exactly one word out of 1119, documented not forced (finding 117) |
| `2026-08-28-binder-segnum-with` | `SYSTEM.COMPILER` 1.3 on `BINDER.text` with the `SEGNUM` loop's `DICT.ADDRLEN[SEGNUM]` reads wrapped in a real `WITH` (matching the disassembly's own `SRO 1121` address-reuse across `LOADSEG`/`WRITESEG`) | **0 errors**; structurally faithful fix, `4/2236` unchanged (no regression, doesn't close finding 117's one-word gap). Chased the gap itself using `SYSTEM.COMPILER`'s own reconstructed `DECLARAT.text` and every local offset `READGOTOXY` touches; ruled out the `FINIT` window address and a second hidden `CONCAT` temp as candidates. No candidate left open (finding 118) |
